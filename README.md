# Milk Farm Management (Prototype)

Simple Vite + React prototype for milk collection and processing business financial management. Stores data in `localStorage`.

Run:

```bash
npm install
npm run dev
```
